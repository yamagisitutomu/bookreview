<div class= 'container'>
  <div class='row'>
    <div class= 'col-md-12 custom-backimage-text'>
    　   <h4 class="text-center">
    　     ようこそ。bookreviewへ！</br>
    　     このサイトは、本のレビューサイトです。</br>
    　     会員でない方も、本の閲覧はできますが、</br>
    　 　   本の評価には、会員登録が必要になります。
    　   </h4>
    </div>
    <div class= 'col-md-6 backimage-text'></div>
  </div>
</div>
<div class='row justify-content-center'>
<%= render 'public/books/search' %>
</div>
<div class='row justify-content-center'>
<%= render "post_searches/form" %>
</div>
<div class='row justify-content-center'>
  <h4>タグ一覧</h4>
  <ul class="tag-list">
    <% @tags.each_slice(4) do |tag_group| %>
      <div class="row">
        <% tag_group.each do |tag| %>
          <div class="col">
            <li><%= link_to tag.name, tag_path(tag) %></li>
          </div>
        <% end %>
      </div>
    <% end %>
  </ul>
</div>